// Inline >>no quote link with hover preview
// Props: { no, boardId, threadNo }
// Renders: clickable >>no text
// On hover: shows preview of referenced post content
// On click: scrolls to referenced post if on same page
