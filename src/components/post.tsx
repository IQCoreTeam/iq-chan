// Single post render — used for both OP and replies
// Props: { no, com, name, time, isOp? }
// Renders: post number, author name, timestamp, comment body
// Comment body parsed via segmentPostBody() for >>no quote links
// Shows edit/delete buttons if post author matches connected wallet
