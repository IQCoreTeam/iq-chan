// Both image URLs use the same validation, renderer, cache and error handling.
export { GET } from "../../share/[...segments]/route.server";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";
